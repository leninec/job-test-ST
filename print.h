/*
 * print.h
 *
 *  Created on: 28 сент. 2019 г.
 *      Author: Vasilii
 */

#ifndef PRINT_H_
#define PRINT_H_

//#include <iostream>
#include <vector>
#include "func.h"
#include <iomanip>
#include <istream>
#include <ctype.h>

using namespace std;

/*
string GetStringAnswer(const vector<int>& vect){
	 stringstream s;
	 s<<"[";
	       	    for (int c = 0 ; c<vect.size() ; ++c){
	       	    		if(c<(vect.size()-1)){
	       	    			s<<vect[c]<<",";
	       	    		}
	       	    		else{
	       	    			s<<vect[c];
	       	    		}
	       	    }
	       	    s<<"],";
	       	    s<<vect.size();
	       	    return s.str();
}
*/
#endif /* PRINT_H_ */
